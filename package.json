{
  "name": "myblog",
  "version": "1.0.0",
  "description": "Blog for Cloudflare Pages",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/ && cp -r views/* dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ejs": "^3.1.9"
  }
}