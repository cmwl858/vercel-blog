{
  "name": "myblog",
  "version": "1.0.0",
  "description": "Blog for Cloudflare Pages",
  "scripts": {
    "build": "if not exist dist mkdir dist && xcopy /s /e /y public dist\\ && xcopy /s /e /y views dist\\"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ejs": "^3.1.9"
  }
}